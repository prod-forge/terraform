# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Terraform state files
terraform.tfstate
terraform.tfstate.backup

# Terraform lock
.terraform.lock.hcl

# Local .terraform directories
.terraform/

# Crash logs
crash.log

# Override files (can contain sensitive info)
override.tf
override.tf.json

# Optional lock file (ignore if you want fixed provider versions in repo)
# .terraform.lock.hcl

service.json
vpn-key
openvpn
ssh

node_modules/
